<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>SnackAttack | Your Dashboard</title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body><%- include('../partials/navbar') %>
<%- include('../partials/content-wrapper') %>

<div class="container mt-5">
  <h2>Edit Recipe</h2>
  <form action="/recipes/<%= recipe._id %>?_method=PUT" method="POST">
    <div class="mb-3">
      <label class="form-label">Title</label>
      <input type="text" name="title" class="form-control" value="<%= recipe.title %>" required>
    </div>

    <div class="mb-3">
      <label class="form-label">Category</label>
      <input type="text" name="category" class="form-control" value="<%= recipe.category %>">
    </div>

    <div class="mb-3">
      <label class="form-label">Ingredients (comma separated)</label>
      <input type="text" name="ingredients" class="form-control" value="<%= recipe.ingredients.join(', ') %>">
    </div>

    <div class="mb-3">
      <label class="form-label">Directions (comma separated)</label>
      <input type="text" name="directions" class="form-control" value="<%= recipe.directions.join(', ') %>">
    </div>

    <h5>Nutritional Info</h5>
    <div class="row">
      <div class="col-md-3 mb-3"><input type="number" name="nutritionalInfo[calories]" class="form-control" value="<%= recipe.nutritionalInfo.calories || 0 %>"></div>
      <div class="col-md-3 mb-3"><input type="number" name="nutritionalInfo[protein]" class="form-control" value="<%= recipe.nutritionalInfo.protein || 0 %>"></div>
      <div class="col-md-3 mb-3"><input type="number" name="nutritionalInfo[carbs]" class="form-control" value="<%= recipe.nutritionalInfo.carbs || 0 %>"></div>
      <div class="col-md-3 mb-3"><input type="number" name="nutritionalInfo[fat]" class="form-control" value="<%= recipe.nutritionalInfo.fat || 0 %>"></div>
    </div>

    <button class="btn btn-primary">Update Recipe</button>
  </form>
</div>
<%- include('../partials/content-wrapper-end') %>

<%- include('../partials/footer') %>
